#include<stdio.h>

void main()
{
   /* int i,a,b,m,k=0,j=0;
    printf("enter two number\t");
    scanf("%d%d",&a,&b);
    	for(i=a;i<=b;i++)
    	{
    		m=i;
    		if(b)
    		while(m>=5)
    		{
    			k=m/5;
    			m=k;
    			j=j+k;
    		}
    	}
    	printf("Sum of no. of zero in the range of %d! and %d! is %d\n",a,b,j);*/

    	int i,n;
    	for(i=1;i<10000;i++)
            printf("%d %d ",i,i+1);
}


