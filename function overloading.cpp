#include <bits/stdc++.h>
using namespace std;

int add(int a, int b)
{
    int sum;
   sum=a+b;
   cout<<sum;
}
int add(float a, float b)
{
    float sum;
    sum=a+b;
   cout<<sum;
}

int  add(char a, char b)
{
    int sum;
    sum=a+b;
   cout<<sum;
}

int add(int a, float b)
{
    float sum;
    sum=a+b;
   cout<<sum;
}

int main()
{
    float c=5.8;
    int d=3;
    add(d,c);
}
